# 問題画像を導入するための方針

問題画像は、**問題データに画像本体を埋め込まず、HTTPSで取得できる画像URLと代替テキストを保持する方式**が適しています。現在の静的フロントエンドでも導入しやすく、JSONファイルが大きくなり過ぎず、ブラウザのローカルストレージ容量を圧迫しません。

## 推奨JSON形式

各問題に任意の `image` オブジェクトを追加します。選択肢にも画像を付けたい場合は、同じ形式を各選択肢に追加できます。既存の画像なし問題はそのまま有効にし、`image` がある場合だけ表示する後方互換の実装にします。

```json
{
  "id": "q-01",
  "type": "single_choice",
  "question": "図のうち正しいものを選んでください。",
  "image": {
    "url": "https://images.example.com/biology/cell-01.webp",
    "alt": "細胞の模式図"
  },
  "choices": [
    {
      "id": "a",
      "text": "細胞膜",
      "image": {
        "url": "https://images.example.com/biology/choice-a.webp",
        "alt": "細胞膜を示す選択肢の図"
      }
    }
  ]
}
```

| 項目 | 推奨 | 理由 |
|---|---|---|
| URL | `https://` の公開URL | JSONが軽く、複数セットで同じ画像を再利用できます。 |
| 代替テキスト | `alt` を必須化 | 画像を読み込めない場合や支援技術でも内容を伝えられます。 |
| 形式 | WebPを優先、PNG/JPEGも許可 | 視認性とファイル容量のバランスを取れます。 |
| サイズ | 1枚あたり2MB以下、長辺2000px以下 | 学習中の表示速度と端末容量への影響を抑えます。 |

## 保存先の選択

最初の段階では、画像を信頼できるCDN、クラウドストレージ、または学校・組織の公開サーバーへ置き、そのURLをJSONに書く方法が最も簡潔です。アプリ側では `https://` のURLだけを許可し、画像の読み込みに失敗した場合は代替テキストとエラー表示を出す設計にします。

画像をアプリ内でアップロード・管理したい段階では、ストレージ機能を備えた構成へ移行します。画像ファイルをオブジェクトストレージへ保存し、JSONには恒久的な画像キーまたは配信URLだけを保存します。アクセス制御が必要な教材は、期限付きURLまたはログインに連動した配信を採用します。

> Base64やData URLとして画像本体をJSONへ埋め込む方式は、ファイルサイズ、読み込み速度、ローカルストレージ容量、編集のしやすさの面で推奨しません。

## 導入手順

次の実装では、まずJSON検証へ任意の `image` フィールドを追加し、問題文・選択肢の直下に画像を表示します。その後、画像URLのHTTPS検証、読込失敗時の代替表示、画像の拡大表示を順に追加すると、安全に機能を拡張できます。
