# 動作確認メモ

- 初期画面では、ロゴ、ダークテーマのインデックスレール、JSON読み込み導線、抽象アトラス背景が表示された。
- 初期状態のセット数は0であり、問題セット未登録時の案内文が表示された。
- 全5種の問題形式を含む `quiz-flow-check.json` を、インポートから結果表示まで確認するために準備した。

確認用JSONをインポートしたところ、タイトル「操作確認テスト」と5問の問題数がサイドレールとセット概要へ反映され、保存成功の通知も表示された。セット概要には挑戦回数、直近の正答率、挑戦履歴の空状態、演習開始の導線が表示された。

演習開始後、1問目の単一選択問題が問題番号、形式、カテゴリ、難易度、タグとともに表示された。選択肢を選ぶと、回答済みカウンタが更新され、選択状態も強調された。

最初の問題を誤答として確定すると、誤答と正答の選択肢が区別して表示され、不正解の通知と解説がその場で開示された。次の問題では複数選択形式の選択肢と、複数選択の状態を示すチェック枠が表示された。

複数選択問題では、JavaScriptとPythonを同時に選択でき、両方の選択状態が視覚的に維持されることを確認した。

複数選択の正解を確定すると、正解の通知と解説が表示された。次の○×問題では、TRUEとFALSEの2択が大きく区別され、問題の形式表示も○ / ×に切り替わった。

○×問題でTRUEを選択し回答を確定すると、正解表示と説明が表示された。

短文回答問題ではテキスト入力欄が表示され、前後に空白を含む「80」を入力して回答状態に反映された。

短文回答を確定すると、空白を除いた正答として採点され、解説が表示された。最終問題では穴埋め形式として同じ入力欄を使用する問題が表示された。

穴埋め問題に「アプリケーション」と入力して確定すると正解・解説が表示され、最終結果へ進む操作が現れた。

結果画面では、4/5問正解・正答率80%・誤答1問が表示され、サイドレールにも挑戦回数1回と80%が反映された。「誤答だけをやり直す」を実行すると、誤答だった単一選択問題だけに絞られ、問題番号が1/1となることを確認した。

ブラウザを再読み込みした後も、問題セット「操作確認テスト」と、挑戦回数1回・正答率80%がサイドレールに保持された。

黒背景の無彩色テーマで、セット概要の「出題設定」から問題順と選択肢順のシャッフル設定が表示されることを確認した。設定画面には、この問題セットに保存されることが明示されている。

問題順と選択肢順の両方を有効にすると、それぞれのスイッチ状態がオンへ変化した。

ブラウザの再読み込み後も、問題順と選択肢順の両方のスイッチがオンのまま表示され、セット別の設定値が保持された。

「出題設定」ボタンをクリックすると、問題順と選択肢順のシャッフル設定を含むパネルが表示された。

同じ「出題設定」ボタンを再度クリックするとパネルが閉じた。シャッフル設定を有効にした状態で開始すると、問題の順序が変わった状態で演習が開始された。

短文回答に誤答を入力して確定すると、入力欄が赤系になり、不正解の通知と解説が赤系のフィードバック面で表示された。

セット概要では挑戦回数に「通常演習のみを集計」と表示され、挑戦履歴には誤答復習を各セッション内へまとめる方針が表示された。出題設定パネルも引き続き同じボタンで開閉できる。

不足選択の採点確認に向け、問題順と選択肢順のシャッフルをオフにして、確認用の出題順を固定した。

固定順の演習を開始したところ、最初に単一選択問題が表示された。次の問題で複数選択の不足項目を確認できる状態になった。

単一選択の正答を確定すると、選択肢と正解フィードバックが控えめな緑系で表示され、チェックアイコンと解説も併記された。

固定順の複数選択問題でJavaScriptのみを選択し、Pythonを未選択にした状態で採点できることを確認した。

採点後、未選択だった正解のPythonは赤系の表示になり、「未選択の正解」と明示された。不正解フィードバックも赤系で表示された。

ホームでは読み込み日が日付のみで表示され、挑戦履歴の補助文は非表示になった。履歴の「誤答を復習」から該当セッションの誤答だけを開始でき、解答画面には「1 / 1・復習」の表示と進行度バーが出ることを確認した。

解答画面の戻る操作では、戻るボタン直下に小さな確認ポップアップが表示された。「続ける」を選ぶと演習画面を維持したままポップアップのみが閉じた。

履歴の誤答復習を開始すると、単一選択の誤答問題が1問だけ表示され、進行度は0%から開始された。

単一選択で大阪を選んで不正解を確定すると、選んだ大阪は赤背景、正解の東京は緑背景で表示された。

復習で誤答を記録した後に同じセッションの「誤答をやり直す」を選ぶと、未解決の東京問題だけが再出題された。

同じ復習で東京に正答すると、正解表示とともに復習を完了できる状態になった。次回の復習対象はこの正解結果を基準に更新される。

復習完了後、結果画面の「誤答をやり直す」は無効状態となり、正解済みの東京問題が同一セッションの次回復習対象から除外されたことを確認した。

初回結果固定の仕様へ変更後、ホームには初回挑戦の結果だけが表示され、復習結果の明細は表示されなかった。同じ履歴から初回の誤答である東京問題を再度「誤答を復習」として開始できた。
